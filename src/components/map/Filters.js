import React from 'react';
import { withRouter } from 'react-router';

class Filters extends React.Component {
  render() {
    return (
      <h1>Show map filters</h1>
    );
  }
}

export default withRouter(Filters);