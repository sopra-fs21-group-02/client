import React from 'react';
import { withRouter } from 'react-router';

class NewTag extends React.Component {
  render() {
    return (
      <h1>Adding page of a Tag </h1>
    );
  }
}

export default withRouter(NewTag);